
import React from 'react';

const Enrollment = () => {
    return (
        <div>
            <h1>Enrollment</h1>
            {/* Add components to manage enrollment */}
        </div>
    );
};

export default Enrollment;
